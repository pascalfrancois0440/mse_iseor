{
  "name": "mse-diagnostic-app",
  "version": "1.0.0",
  "description": "Application de diagnostic socio-économique selon la méthodologie ISEOR pour consultants MSE",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["MSE", "ISEOR", "diagnostic", "coûts-cachés"],
  "author": "MSE Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
